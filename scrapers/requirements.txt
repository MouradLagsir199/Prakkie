# Scraper + bronze-ingest dependencies.
# Install into a venv:  py -m venv .venv && .venv/Scripts/python -m pip install -r scrapers/requirements.txt
httpx[http2]>=0.27,<1
curl-cffi>=0.7,<1
psycopg[binary]>=3.1,<4
